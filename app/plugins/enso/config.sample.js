module.exports = {
    // other config properties
    plugins: {
        // config for other plugins
        // ...
        script: `${__dirname}/../plugins/enso/index.js`,
        command: {
        }
    }
};
